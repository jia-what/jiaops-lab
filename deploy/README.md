# Deploy

部署相关配置按阶段逐步补充：

- `compose/` — Docker Compose（Phase 3）：见 [compose/README.md](compose/README.md)
- `nginx/` — 反向代理：裸机 `jiaops.conf` · Compose `jiaops-docker.conf`
- `k8s/` — Kubernetes 清单（后期）
