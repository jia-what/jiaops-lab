# 复制为 .env 后填写（.env 不要提交到 Git）
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=1

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=jiaops
MYSQL_PASSWORD=Jiaops@123
MYSQL_DATABASE=jiaops

# Redis（可选；不填则 app 不连 Redis）
# Compose 栈：REDIS_HOST=redis
# 裸机：REDIS_HOST=127.0.0.1（需本机已装 Redis）
REDIS_HOST=
REDIS_PORT=6379
